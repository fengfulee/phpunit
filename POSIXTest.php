<?PHP

/**
 * POSIX ---POSIX-extended regular expression.
 * PHP also supports regular expressions using a Perl-compatible syntax using the
 * PCRE functions. Thosee functons supports non-greedy matching, assertions,
 * conditional subpatterns, and a number of other features not supported by the 
 * POSIX-extended regular expression.
 */
class POSIXTest extends PHPUnit_Framework_TestCase {

	/**
	 * Well , function `ereg_replace`, `ereg`, `eregi_replace`, `eregi`, 
	 * `split`, `spliti`, `sql_regcase` all had been deprecated as of
	 * PHP 5.3.0.
	 */
}
?>
